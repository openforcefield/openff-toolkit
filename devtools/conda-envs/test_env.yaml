name: test
channels:
  - conda-forge
  - omnia

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - nbval
  - codecov
  - coverage=4.4
  - numpy
  - networkx
  - parmed
  - packaging
  - openmmtools
  - pyyaml
  - toml
  - bson
  - msgpack-python
  - xmltodict

    # Pip-only installs
  #- pip:

