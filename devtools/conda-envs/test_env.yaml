name: test
channels:
  - conda-forge
  - omnia

dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - nbval
  - codecov
  - coverage < 5.0
  - numpy
  - networkx
  - ambertools
  - packaging
  - openmmtools
  - openforcefields
  - smirnoff99Frosst
  - pyyaml
  - toml
  - bson
  - msgpack-python
  - xmltodict
  - qcelemental
  - qcportal

    # Pip-only installs
  #- pip:

