name: test
channels:
  - conda-forge
  - omnia
  - openeye

dependencies:
    # Base depends
  - openff-toolkit
    # Removed until a new release which targets openff-toolkit is made.
#  - openmmforcefields
  - openeye-toolkits
  - pytest
  - nbval