name: test
channels:
  - conda-forge
  - openeye

dependencies:
    # Base depends
  - openff-toolkit
    # Removed until a new release which targets openff-toolkit is made.
#  - openmmforcefields
  - openmm
  - openeye-toolkits
  - pytest
  - nbval