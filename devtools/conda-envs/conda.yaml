name: latest-deployment
channels:
  - conda-forge

dependencies:
    # Base depends
  - openff-toolkit-examples

  - openmmforcefields
  - qcportal >=0.15
  - pytest
  - pytest-rerunfailures
  - nbval
